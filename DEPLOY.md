# 公開手順書（お名前.com でサイトを公開する）

株式会社公雅建設サイトを、お名前.com で取得したドメイン＋レンタルサーバーで公開するための手順です。
上から順に実施すれば公開できます。**所要時間は作業30分＋DNS反映待ち（数時間）** が目安です。

> 📖 **「なぜこの作業が必要なのか」「このファイルは何なのか」は [LEARN.md](LEARN.md) に解説をまとめています。**
> この手順書が「何をやるか」、LEARN.md が「なぜやるか」です。並べて読むと理解しやすくなります。

---

## 0. 事前に手元に用意するもの

| 必要なもの | どこで確認するか |
|---|---|
| 取得したドメイン名 | お名前.com Navi の「ドメイン一覧」 |
| お名前.com Navi のログイン情報 | 契約時のメール（お名前ID＋パスワード） |
| レンタルサーバーのコントロールパネル ログイン情報 | 契約時のメール（件名に「レンタルサーバー」が入っているもの） |
| FTPソフト | 未インストールなら [FileZilla](https://filezilla-project.org/) を入れておく（無料） |
| アップロードするファイル | このフォルダの `site/` の中身（後述） |

> 💡 お名前.comには「レンタルサーバー（RS）」「共用サーバーSD」「VPS」など複数のプランがあります。
> 本手順は**レンタルサーバー（RS）**を前提に書いています。画面の名称が違う場合は 手順6（FTPアップロード）だけ共通なので、そこを目指してください。

---

## 1. サーバーにドメインを登録する

1. レンタルサーバーの**コントロールパネル**にログイン
2. メニューから **「ドメイン」→「ドメイン設定」→「ドメインの追加」** を開く
3. 取得したドメイン名を入力して追加

追加すると、そのドメイン専用の**公開フォルダ（ドキュメントルート）**が作られます。
このパスを**メモしてください**（手順6で使います）。表記例：

```
/ドメイン名/public_html/
```

---

## 2. ドメインのネームサーバーをサーバーに向ける

「ドメインを買ったサーバーに紐付ける」作業です。**ここを飛ばすとサイトは表示されません。**

1. **お名前.com Navi** にログイン
2. **「ドメイン」→「ネームサーバーの設定」→「ネームサーバーの変更」**
3. 対象ドメインにチェックを入れる
4. **「お名前.comのサービスを利用する」**（または「レンタルサーバー」等の項目）を選び、契約中のレンタルサーバーを指定
5. 確認して設定を保存

> ⏱ **反映には数時間〜最大72時間かかります**（多くの場合3〜6時間程度）。
> この間はサイトが「表示されない／古い画面が出る」状態が正常です。焦らず待ってください。

**反映されたかの確認方法：** コマンドプロンプトで以下を実行し、サーバーのネームサーバー（`*.dnsv.jp` など）が返ってくればOKです。

```bash
nslookup -type=ns ドメイン名
```

---

## 3. 無料SSL（https）を有効化する

⚠️ **手順2のDNS反映が完了してから実施してください。**未反映のまま実行すると失敗します。

1. コントロールパネルの **「セキュリティ」→「SSL」→「無料SSL設定」**（または「独自SSL」）
2. 対象ドメインを選んで **「利用する」→ 設定**
3. 発行完了まで **10分〜1時間**ほど待つ

有効化後は `https://ドメイン名/` でアクセスできるようになります。
（`http://` でのアクセスは、後述の `.htaccess` により自動で `https://` へ転送されます）

---

## 4. アップロードするファイルを確認する

アップロードするのは **`site/` フォルダの「中身」** です。`site` フォルダごとではありません。

```
site/                    ← このフォルダ自体はアップロードしない
├── index.html           ┐
├── service.html         │
├── works.html           │
├── company.html         │
├── recruit.html         │  この中身すべてを
├── contact.html         │  サーバーの公開フォルダ直下に置く
├── 404.html             │
├── favicon.svg          │
├── robots.txt           │
├── .htaccess            │ ← 隠しファイル。忘れやすいので注意
├── css/                 │
├── js/                  │
└── images/              ┘
```

**アップロード後のサーバー側の状態（正しい例）:**

```
/ドメイン名/public_html/
├── index.html      ← ここに index.html が来ていればOK
├── css/
├── js/
└── images/
```

❌ `public_html/site/index.html` になっていると `https://ドメイン名/site/` でしか見られません。

---

## 5. FTPの接続情報を確認する

コントロールパネルの **「FTP」→「FTPアカウント」** で以下を確認・メモします。

- **FTPサーバー名（ホスト名）**： 例）`sv◯◯.onamae.ne.jp`
- **FTPユーザー名**
- **FTPパスワード**（不明ならここで再設定できます）

---

## 6. FileZilla でファイルをアップロードする

1. FileZilla を起動
2. 上部に接続情報を入力して **「クイック接続」**
   - ホスト： `ftps://FTPサーバー名` ← **`ftps://` を付けると暗号化接続になります（推奨）**
   - ユーザー名 / パスワード： 手順5のもの
   - ポート： 空欄でOK
3. **隠しファイルを表示する設定にする**
   → メニュー **「サーバー」→「強制的に隠しファイルを表示する」にチェック**
   （これをしないと `.htaccess` がアップロードされません）
4. 画面**右側**（サーバー側）で、手順1でメモした公開フォルダ（`/ドメイン名/public_html/`）を開く
5. 画面**左側**（自分のPC側）で `G:\マイドライブ\デモサイト\koga-kensetsu-hp\site` を開く
6. 左側のファイル・フォルダを**すべて選択**して、右側へドラッグ＆ドロップ
7. 転送が終わるまで待つ（画像が多いので数分かかります）

---

## 7. 表示を確認する

ブラウザで `https://ドメイン名/` を開き、以下をチェックしてください。

- [ ] ローディング演出（クレーンが「春」ロゴを吊り上げる）が出る
- [ ] トップの写真スライドショーが切り替わる
- [ ] アドレスバーに **鍵マーク**が付いている（SSL有効）
- [ ] `http://ドメイン名/` を開くと `https://` に自動で切り替わる
- [ ] ヘッダーの各メニュー（事業内容／施工実績／会社概要／採用情報／お問い合わせ）が開ける
- [ ] 施工実績ページで写真が表示され、カテゴリ絞り込み・写真拡大が動く
- [ ] **スマホ**でも開いて、横スクロールが出ないか確認
- [ ] ブラウザのタブに「春」のアイコンが出る
- [ ] 存在しないURL（例 `https://ドメイン名/aaa`）で404ページが出る

### うまくいかないときの対処

| 症状 | 原因と対処 |
|---|---|
| 「このサイトにアクセスできません」 | DNS未反映。手順2の待ち時間。数時間待つ |
| サーバーの初期画面が出る | ファイルが公開フォルダ直下に無い。手順4の配置を確認 |
| 文字だけ表示されデザインが崩れる | `css/` フォルダが未アップロード。再アップロード |
| 写真が表示されない | `images/` が未アップロード、またはファイル名の大文字小文字違い（サーバーは区別します） |
| httpsにならない | 手順3のSSL未発行、または `.htaccess` が未アップロード |
| 500エラーが出る | `.htaccess` が原因。一旦リネームして切り分ける |

---

## 8. お問い合わせフォームを有効にする（重要）

このサイトのフォームは **FormSubmit** というサービス経由で `baru.k.t1975@ozzio.jp` にメールが届く設定になっています。
**初回だけ、有効化の作業が必要です。**

1. 公開したサイトの **お問い合わせページ**を開く
2. テスト内容を入力して**実際に送信する**
3. `baru.k.t1975@ozzio.jp` に **FormSubmit から確認メール**が届く
4. メール本文の**有効化リンクを1回クリック**する

これで以降の送信が正常にメール受信されます。
**4を行うまでメールは届きません**ので、公開後すぐに実施してください。

> 📌 有効化のあと、もう一度テスト送信して受信できることを必ず確認してください。
> 迷惑メールフォルダに入ることがあるので、そちらも確認を。

---

## 9. 仕上げ（公開後にやっておくとよいこと）

### 9-1. sitemap.xml と canonical の設定
検索エンジンに正しく登録されるための設定です。**ドメイン名が確定したら対応します**ので、
決まったドメイン名を教えてください（`sitemap.xml` の生成と各ページへの canonical / OGP 設定を行います）。

### 9-2. Google Search Console に登録
1. [Google Search Console](https://search.google.com/search-console) にアクセス
2. 「プロパティを追加」→「URLプレフィックス」→ `https://ドメイン名/` を入力
3. 所有権の確認（HTMLファイルをアップロードする方法が簡単）
4. サイトマップを送信

### 9-3. 名刺・見積書などにURLを記載
公開したURLを名刺や書類に載せる場合は、`https://` から始まる形で記載してください。

---

## 10. 今後サイトを更新するとき

### 方法A：手動アップロード（確実・おすすめ）
1. ローカル（Gドライブ）のファイルを修正
2. FileZilla で**変更したファイルだけ**上書きアップロード

### 方法B：GitHub に push すると自動アップロード（任意・上級）
`.github/workflows/deploy.yml` を用意済みです。GitHubにFTP情報を登録すると、
`git push` するだけで自動的にサーバーへ反映されます。

**設定方法：**
1. GitHubリポジトリ → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**
2. 以下4つを登録

| Name | Value |
|---|---|
| `FTP_SERVER` | FTPサーバー名（`ftps://` は付けない） |
| `FTP_USERNAME` | FTPユーザー名 |
| `FTP_PASSWORD` | FTPパスワード |
| `FTP_SERVER_DIR` | 公開フォルダのパス。**末尾に `/` を付ける**（例 `/ドメイン名/public_html/`） |

登録前は自動アップロードはスキップされる（何も起きない）ので、安全です。

---

## 補足：GitHub Pages との使い分け

現在、確認用として GitHub Pages でも公開されています。

| | URL | 用途 |
|---|---|---|
| 本番 | `https://ドメイン名/` | お客様・取引先に案内する正式サイト |
| 確認用 | https://fshin927.github.io/koga-kensetsu-hp/site/index.html | 修正内容の事前確認 |

修正時はまず確認用で見てから本番に反映する、という進め方ができます。
（確認用を残したくない場合は、GitHubの Settings → Pages から無効化できます）
