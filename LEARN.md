# 解説：ホームページ公開のしくみと、各作業・各ファイルの意味

「なぜこの作業が必要なのか」「このファイルは何のためにあるのか」を、一つずつ説明します。
`DEPLOY.md`（手順書）が「何をやるか」、この文書が「なぜやるか」です。並べて読んでください。

---

# 第1部　そもそもホームページはどうやって見えているのか

ここを理解すると、あとの作業の意味がほぼ全部わかります。

## 1-1. ホームページの正体は「ファイルの集まり」

ホームページは特別なものではなく、**ただのファイル**です。

- 文章と構造を書いたファイル … `index.html` など（HTML）
- 見た目を指定するファイル … `style.css`（CSS）
- 動きを指定するファイル … `main.js`（JavaScript）
- 写真 … `images/` の中のJPEGファイル

Wordファイルをダブルクリックすると Word が開いて中身を表示するのと同じで、
**HTMLファイルはブラウザが開いて表示します。** 実際、今Gドライブにある `site/index.html` を
ダブルクリックすれば、そのままサイトが表示されます。

つまり「公開する」というのは、**このファイル一式を、誰でも取りに来られる場所に置く**という作業に過ぎません。

## 1-2. サーバーとは「電源を切らないパソコン」

いまファイルはあなたのPCの中にあります。でもあなたのPCの電源を切ったら誰も見られません。
そこで、**24時間365日電源が入っていて、ネットにつながっているパソコン**を借ります。これが**サーバー**です。

お名前.comに毎月払っているのは、この「常時稼働のパソコンの一部を貸してください」という料金です。
（レンタルサーバーは1台を大勢で分けて使うので安価です）

> **たとえると：** サーバー＝土地・建物を借りること。

## 1-3. ドメインとは「住所」

サーバーには本来 `203.0.113.25` のような**IPアドレス**という番号が付いています。
これがサーバーの本当の居場所です。でも番号は覚えられません。

そこで、覚えやすい名前を別に買います。これが**ドメイン**（`example.com` など）です。

> **たとえると：** ドメイン＝住所・看板。

ここが最初のつまずきポイントですが、**ドメインとサーバーは別々の商品**です。
同じお名前.comで買っても、契約としては無関係です。

- ドメインだけ買っても、置く場所がないので何も表示できません
- サーバーだけ借りても、覚えやすい名前がないので番号でしかアクセスできません

**両方買って、さらに「この住所はこのサーバーです」と結びつける作業が必要** — これが手順1・2です。

## 1-4. ネームサーバー（DNS）とは「住所録」

「ドメイン名 → どのサーバーか」を答えてくれる、インターネット全体の巨大な電話帳が **DNS** です。
その電話帳に載っている情報の管理担当が **ネームサーバー** です。

上の図の流れをもう一度言葉にすると：

1. お客様がブラウザにドメイン名を入力する
2. ブラウザがネームサーバーに「この住所はどのサーバー？」と聞く
3. ネームサーバーが「このIPアドレスです」と答える
4. ブラウザがそのサーバーに「ファイルをください」と頼む
5. サーバーが `index.html` などを返す
6. ブラウザがそれを組み立てて画面に表示する

**手順2（ネームサーバーの変更）は、この「3」で正しい答えが返るようにする作業**です。
ここを設定しないと、ネームサーバーは「そんな住所は知りません」と答えるので、
ブラウザには「このサイトにアクセスできません」と出ます。**何度リロードしても直りません。**

---

# 第2部　公開作業の一つ一つの意味

## 手順1　サーバーにドメインを登録する

**やること：** サーバーの管理画面で、自分のドメイン名を登録する。

**なぜ必要か：**
レンタルサーバーは1台を大勢で共有しています。サーバーに `example.com` へのアクセスが届いても、
サーバー側は「その名前は誰の分？どのフォルダを見せればいい？」がわかりません。

登録することで、サーバーの中に**そのドメイン専用の公開フォルダ**が用意され、
「`example.com` で来た人にはこのフォルダの中身を見せる」というルールができます。

> **たとえると：** 借りた建物の入口に「ここが example.com の部屋です」と表札を出す作業。

## 手順2　ネームサーバーを変更する

**やること：** お名前.com Navi で、ドメインのネームサーバーを、契約したレンタルサーバーのものに変える。

**なぜ必要か：** 1-4 のとおり、住所録に「この住所の建物はここ」と登録する作業です。
**この設定なしにサイトが表示されることは絶対にありません。**

### なぜ数時間も待つのか

DNSの情報は、世界中のプロバイダやルーターが**一時的にコピーを保存（キャッシュ）**しています。
毎回大元に問い合わせると重くなるので、「しばらくは覚えておいて使い回す」しくみです。

設定を変えても、各所に残っている古いコピーが期限切れになるまでは古い情報が使われます。
この入れ替わりを待つのが「DNS反映待ち」で、**通常3〜6時間、長いと最大72時間**かかります。

**ここで大事なこと：** 待っている間に「表示されない」のは**正常**です。
故障を疑って設定を触ると、かえって原因がわからなくなります。**待つのも作業です。**

## 手順3　無料SSLを有効化する

**やること：** 管理画面で「無料SSL」を有効にする。

**SSLとは：** ブラウザとサーバーの間の通信を**暗号化**するしくみです。
有効にすると、URLが `http://` ではなく `https://` になり、アドレスバーに鍵マークが付きます。

**なぜ必要か（3つあります）：**

1. **お問い合わせ内容を守るため。**
   `http://` のままだと、通信内容が**そのままの文字**でネットを流れます。
   途中の経路（特に公衆Wi-Fiなど）で盗み見られる可能性があります。
   このサイトはフォームで**名前・電話番号・メールアドレス**を送るので、暗号化は必須です。

2. **ブラウザが警告を出すため。**
   いまのChromeやSafariは、`https://` でないサイトに「保護されていない通信」と表示します。
   取引先が見たときの印象として、これは避けたいところです。

3. **検索順位に影響するため。**
   Googleは `https://` を評価要素にすると公表しています。

> **たとえると：** はがき（http）で送るか、封筒に入れて封をして（https）送るか、の違い。

**注意：** 手順2のDNS反映が終わる前にSSLを申し込むと失敗します。
証明書を発行する会社が「本当にこのドメインの持ち主か」をDNS経由で確認するためです。**順番が大事です。**

## 手順4〜6　FTPでファイルをアップロードする

**FTPとは：** 自分のPCとサーバーの間でファイルをやり取りする方法（の名前）です。
FileZilla のようなソフトを使うと、左が自分のPC、右がサーバーの中身として表示され、
**ドラッグ＆ドロップでコピーできます。** Windowsのエクスプローラーとほぼ同じ感覚です。

**なぜ `ftps://` を付けるのか：**
昔からの `ftp` は、**パスワードまで暗号化せずに送ります**。`ftps` は暗号化された安全版です。
サーバーのパスワードが漏れると、サイトを勝手に書き換えられてしまうので、必ず `ftps://` を使ってください。

### ドキュメントルート（公開フォルダ）とは

サーバーの中で、**「ここから下だけを外に見せる」と決められたフォルダ**のことです。
`/ドメイン名/public_html/` のような場所です。

なぜ限定するかというと、サーバーには設定ファイルやログなど**見せてはいけないものも入っている**からです。
公開フォルダの外は、外部から絶対に見えません。

### なぜ「site フォルダごと」ではなく「中身」なのか

ブラウザが `https://example.com/` にアクセスすると、サーバーは
**公開フォルダの直下にある `index.html`** を探します。

- 正しい：`public_html/index.html` → `https://example.com/` で表示される
- 誤り：`public_html/site/index.html` → `https://example.com/site/` でしか表示されない

`site` というのは、あなたのPC上で整理のために付けた**ただのフォルダ名**です。
サーバー上ではその階層は不要なので、中身だけを置きます。

---

# 第3部　ファイルの意味

## 全体の対応表

| ファイル | 役割 | なくなるとどうなるか |
|---|---|---|
| `index.html` | トップページの中身 | サイトが表示されない |
| `service.html` 他 | 各ページの中身 | そのページが404になる |
| `css/style.css` | 見た目の指定 | 文字だけの真っ白なページになる |
| `js/main.js` | 動きの指定 | メニュー・実績一覧・フォームが動かない |
| `js/works-data.js` | 施工実績のデータ | 実績が1件も出ない |
| `images/` | 写真 | 写真が全部欠ける |
| `favicon.svg` | タブのアイコン | タブが白紙アイコンになる |
| `.htaccess` | サーバーへの指示書 | httpsに転送されない・404ページが出ない |
| `404.html` | エラーページ | 素っ気ないサーバー標準画面が出る |
| `robots.txt` | 検索エンジンへの案内 | 大きな支障はない（あった方がよい） |

---

## HTMLファイル（`index.html` など）— 中身と構造

**役割：** ページに「何が書かれているか」を定義します。見出し・段落・画像・リンクなどです。

**なぜ `index` という名前なのか：**
Webサーバーには「フォルダ名だけでアクセスされたら `index.html` を返す」という**古くからの約束事**があります。
だから `https://example.com/` だけで開けます。`index` 以外の名前だと、
`https://example.com/top.html` のようにファイル名まで打たないと開けません。

**なぜページごとにファイルを分けるのか：**
ページごとに別のURLになるので、「事業内容のページだけをメールで送る」ことができ、
Googleにもページ単位で登録されます。1ファイルに全部詰めると、これができません。

## `css/style.css` — 見た目

**役割：** 色・文字の大きさ・余白・配置・アニメーションなど**見た目のすべて**を指定します。

**なぜHTMLと分けるのか：**
このサイトは7ページありますが、デザインは共通です。分けておけば、
**`style.css` を1か所直すだけで全ページのデザインが変わります。**
HTMLに直接書いていたら、7ファイル全部を直す必要があります。

「和紙の質感」「明朝体」「朱色」といった名刺に合わせた設定も、すべてこのファイルの中にあります。

## `js/main.js` — 動き

**役割：** 読み込んだ後に「動く」部分を担当します。具体的には：

- 起動時のローディング演出（クレーンがロゴを吊り上げる）
- トップの写真スライドショーの切り替え
- スクロールに応じて文字がふわっと出る動き
- スマホのハンバーガーメニューの開閉
- 施工実績の一覧表示・カテゴリ絞り込み・写真の拡大
- お問い合わせフォームの入力チェックと送信

HTMLが「骨」、CSSが「見た目」、JavaScriptが「動き」という分担です。

## `js/works-data.js` — 施工実績のデータ

**役割：** 施工実績の内容（工事名・場所・説明・写真ファイル名）だけを集めたファイルです。

**なぜ分けたのか：**
実績は今後増えていきます。HTMLを直接編集するとタグを壊してしまう危険がありますが、
このファイルは**決まった形式のリストが並んでいるだけ**なので、
既存の1件をコピーして書き換えるだけで安全に追加できます。

追加すると、トップページ・施工実績ページ・カテゴリの絞り込みボタンが**すべて自動で更新されます。**

## `images/` — 写真

施工写真です。`works-data.js` や HTML から**ファイル名で呼び出しています。**

**注意点：** サーバーはLinuxで動いているため、**ファイル名の大文字と小文字を別物として扱います。**
`Photo.JPG` と `photo.jpg` は違うファイルです。
（Windowsは区別しないので、「PCでは見えるのにサーバーだと写真が出ない」原因の定番です）

## `favicon.svg` — タブのアイコン

**役割：** ブラウザのタブやブックマークに表示される小さなアイコンです。
名刺の菱形「春」マークをそのまま図形データにしてあります。

**なぜ必要か：** 無くても動きますが、タブがたくさん開いているときに一目で見分けられます。
また、無いと白紙アイコンになり、それだけで「作りかけのサイト」に見えてしまいます。

## `.htaccess` — サーバーへの指示書（重要）

**役割：** **サーバーの動作そのものを変える設定ファイル**です。他のファイルとは性質が違います。

**なぜ名前が「.」で始まるのか：**
Linuxでは、ドットで始まるファイルは**隠しファイル**として扱われます。
設定ファイルは普段目に入らない方が邪魔にならないためです。
このせいで、FileZillaで隠しファイル表示をONにしないと**アップロードし忘れます。**

**このファイルが指示している内容：**

| 設定 | 何のためか |
|---|---|
| `http` → `https` へ転送 | 誰かが `http://` で来ても、自動で暗号化された方に切り替える。名刺やSNSに古いURLが載っていても安全 |
| `ErrorDocument 404` | 存在しないURLのときに、自作の `404.html` を表示させる |
| `Options -Indexes` | `https://example.com/images/` のようにアクセスされたとき、**写真の一覧が丸見えになるのを防ぐ** |
| キャッシュ設定 | 一度見た人のPCに写真を保存させ、2回目以降の表示を速くする |
| gzip圧縮 | ファイルを圧縮して送り、表示を速くする |
| セキュリティヘッダー | ブラウザに安全側の動作をさせる指示 |
| `www` の統一（任意） | `www.example.com` と `example.com` を片方に寄せる |

**注意：** 強力なファイルなので、書き間違えると**サイト全体が500エラーで真っ白になります。**
不具合が出たときは、まずこのファイルの名前を `_htaccess` などに変えて（＝無効化して）切り分けます。

## `404.html` — 存在しないページ用の受け皿

**役割：** 間違ったURLでアクセスされたときに表示するページです。

**なぜ必要か：**
用意しないと、サーバー標準の真っ白な英語のエラー画面が出ます。
取引先がURLを打ち間違えただけで「このサイト、大丈夫か？」と思われてしまいます。

自作の404ページなら、サイトと同じデザインで、
「トップへ戻る」「お問い合わせ」のボタンを置いて**そのまま案内できます。**

**技術的な補足：** 404ページの中で `css/style.css` ではなく `/css/style.css`（先頭にスラッシュ）と
書いているのは、**どんな深い階層のURLで表示されても正しくCSSを読み込めるようにするため**です。

## `robots.txt` — 検索エンジンへの案内

**役割：** Googleなどの巡回プログラム（クローラー）に対して、
「どこを見てよいか」「サイトの地図はどこか」を伝えるファイルです。

置き場所は**公開フォルダの直下で固定**です。クローラーは必ずここを最初に見に来ます。

**補足：** これは「お願い」であって、アクセス制限ではありません。
**見せたくないファイルは、そもそも公開フォルダに置かない**のが正解です。

## これから作るファイル（ドメイン確定後）

| ファイル | 役割 |
|---|---|
| `sitemap.xml` | サイト内の全ページ一覧。Googleが漏れなく見つけられるようにする「地図」 |
| canonical（各HTMLに追記） | 同じ内容が複数URLで見えるときに「正式なURLはこれ」と宣言する。`www`の有無などで**同じページが別ページと誤認され、評価が分散するのを防ぐ** |
| OGP（各HTMLに追記） | LINEやSNSでURLを送ったときに、写真とタイトルが付いたカードで表示されるようにする設定 |

これらは**絶対URL（`https://ドメイン名/...`）で書く必要がある**ため、ドメイン確定後の作業になります。

---

# 第4部　開発側のファイル（サーバーには上げないもの）

## `.git` フォルダと GitHub

**Gitとは：** ファイルの変更履歴を全部記録しておくしくみです。

**なぜ必要か：**
「修正したら前より悪くなった」「昔の状態に戻したい」というときに、**いつでも巻き戻せます。**
上書き保存だと元に戻せません。実際この打ち合わせでも何度も修正しましたが、
すべての段階が記録されているので、どの時点にも戻せます。

**GitHubとは：** その履歴をインターネット上に預けておくサービスです。
PCが壊れてもサイトのデータが失われません。**バックアップとしての意味が大きいです。**

## `DEPLOY.md` / `LEARN.md`

手順書とこの解説です。`.md`（マークダウン）は、**記号で見出しや表を表現できるテキストファイル**です。
ふつうのメモ帳でも開けます。

## `.github/workflows/deploy.yml`

**役割：** 「GitHubに変更を送ったら、自動でサーバーにもアップロードする」という**自動化の指示書**です。

**なぜあると便利か：**
手動だと「修正 → FileZillaを起動 → 該当ファイルを探して上書き」が毎回必要です。
これを設定しておくと、変更を保存して送るだけでサイトが更新されます。
**アップロード忘れ・上げ間違いも防げます。**

いまはFTPのパスワードを登録していないので**何も起きません**（安全に止まっています）。

## `serve.ps1` / `serve-prod.ps1`

**役割：** 自分のPCの中だけで、サーバーと同じようにサイトを表示させる**確認用のプログラム**です。

**なぜ必要か：**
本番サーバーにアップロードしてから確認すると、失敗が公開されてしまいます。
先に手元でまったく同じ構成で確認できれば、**安全な状態だけを公開できます。**

`serve-prod.ps1` の方は、`site/` の中身を公開フォルダの直下に置いた状態を再現しているので、
**アップロード後とまったく同じURL構成**で確認できます。

---

# 第5部　用語ミニ辞典

| 用語 | ひとことで言うと |
|---|---|
| ドメイン | サイトの住所（`example.com`） |
| サーバー | ファイルを置いておく、常時稼働のパソコン |
| IPアドレス | サーバーの本当の居場所を示す番号 |
| DNS / ネームサーバー | ドメイン名からサーバーを調べる住所録 |
| DNS反映 | 住所録の変更が世界中に行き渡るまでの待ち時間 |
| ドキュメントルート | サーバーの中で外部に公開されるフォルダ |
| SSL / TLS | 通信を暗号化するしくみ。`https` になる |
| FTP / FTPS | ファイルをサーバーに送る方法。FTPSは暗号化版 |
| HTML | ページの中身と構造 |
| CSS | ページの見た目 |
| JavaScript | ページの動き |
| キャッシュ | 一度取得したものを一時保存して使い回すしくみ |
| 404 | 「そのページは存在しません」を表す番号 |
| 500 | 「サーバー側で異常が起きた」を表す番号 |
| canonical | 正式なURLはこれ、と宣言するタグ |
| OGP | SNSでシェアされたときの見た目を決める設定 |
| Git / GitHub | 変更履歴を記録・保管するしくみとサービス |

---

# おわりに

もし途中でわからない言葉が出てきたら、そのまま聞いてください。
「なんとなく進める」よりも、**意味がわかった状態で進める方が、後で必ず楽になります。**

公開後もサイトは更新していくものなので、ここの理解は一度しておくと長く役に立ちます。
